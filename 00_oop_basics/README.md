# Phase 0: OOP basics

- **shapes.py** — Inheritance & polymorphism: `Shape` base, `Rectangle`, `Circle`.
- **composition_example.py** — Composition vs inheritance (e.g. Engine in Car).

Run:
```bash
python -m 00_oop_basics.shapes
python -m 00_oop_basics.composition_example
```
